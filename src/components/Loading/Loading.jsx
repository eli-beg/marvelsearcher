import { LoadingSpin, LoadingContainer } from "./styled";

const Loading = () => {
  return (
    <LoadingContainer>
      {" "}
      <LoadingSpin />
    </LoadingContainer>
  );
};

export default Loading;
